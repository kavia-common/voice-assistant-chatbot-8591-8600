# Core Django stack
Django==5.0.6
asgiref==3.8.1
sqlparse==0.5.1
pytz==2024.1

# REST and docs
djangorestframework==3.15.2
drf-yasg==1.21.7
uritemplate==4.1.1
django-cors-headers==4.4.0

# Environment variables
python-dotenv==1.0.1

# LLM / AI
openai==1.52.2
requests==2.32.3

# Speech libs
SpeechRecognition==3.10.3
# Avoid pocketsphinx by default due to heavy system deps; STT falls back to Google Web Speech API.
# pocketsphinx==5.0.3  # optional; requires system libs not present in minimal containers

# TTS
gTTS==2.6.0
# pyttsx3 is pure-Python but may attempt to interface with system audio backends at runtime.
# Keep it installed for offline TTS but handle failures gracefully in code (already done).
pyttsx3==2.98

# Dev/qa tools (kept lightweight)
flake8==7.1.1
mccabe==0.7.0
pycodestyle==2.12.1
pyflakes==3.2.0
packaging==24.1
pluggy==1.5.0
iniconfig==2.0.0
inflection==0.5.1
PyYAML==6.0.1
